name: file_cryptor
description: FileCryptor is for encryption and decryption files. (PDF, MP3, MP4, PNG, ...)
version: 0.0.3
homepage: https://github.com/webdastur/file_cryptor

environment:
  sdk: ">=2.12.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  encrypt: ^5.0.1
  path: ^1.8.3
  meta: ^1.9.1
  archive: ^3.3.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: